export class Customer {
    key: string;
    name: string;
    age: number;
    active = true;
    // car: string[];
    car: any;

    /* public normalize(): void {
        if ((typeof this.car) === 'string'){};
    } */
}

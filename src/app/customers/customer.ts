export class Customer {
    key: string;
    name: string;
    age: number;
    active = true;
    // cars: string[];
    car: any;

    /* public normalize(): void {
        if ((typeof this.cars) === 'string'){};
    } */
}
